name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main  # lub master - zależy od tego, jak nazwałeś swoją główną gałąź

# Pozwala na wdrożenie strony i modyfikację uprawnień tokens
permissions:
  contents: read
  pages: write
  id-token: write

# Blokuje jednoczesne powtarzające się wdrożenia
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Build Application
        run: npm run build

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload Build Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4